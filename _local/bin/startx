#!/usr/bin/env bash

# PIDFILE="~/.local/tmp/xinit/pid"
# rm -rf $PIDFILE
# mkdir -p $(dirname $PIDFILE)
# echo "$$" >> $PIDFILE

termux-x11 :1 -xstartup "~/.xinitrc" &
PID=$!
export DISPLAY=:1
am start com.termux.x11/com.termux.x11.MainActivity

trap "kill -2 $PID" SIGINT

wait $PID
