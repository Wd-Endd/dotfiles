#!/usr/bin/env bash

tmp_dir="$HOME/.local/tmp/lich_am_duong"

if [[ ! -d "$tmp_file" ]]; then
    rm -rf "$tmp_dir"
    mkdir -p "$tmp_dir"
fi

today="$(date +"%d-%m-%Y")"

if [[ ! -f "$tmp_dir/$today" ]]; then
    lich_duong="$(date +"%a %d %b %Y")"
    lich_am="$(curl lich.day | grep Năm)"
fi
